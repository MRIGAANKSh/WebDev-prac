<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body class="bg-zinc-900 text-white">
  <div class="w-full min-h-screen p-10 ">
    <div class="nav mb-10 text-blue-500 ">
        <a href="/">Create User</a>
    </div> <% if(allusers.length>0){%>
    <h1 class=" text-3xl  mt-2 bold">All Users</h1>
    <div class="users mt-2 flex gap-3 flex-wrap">
       
            <% allusers.forEach(function(user){ %>
                <div class="user max-w-72 p-4 bg-zinc-700 rounded-lg flex  flex-wrap ">
                <div class="w-full h-52 rounded-lg bg-zinc-500  overflow-hidden">
                    <img class="w-full h-full object-cover" src="<%=user.image%>" alt="here is iame">
                </div>
                <h3 class="text-xl mt-3 tracking-tighter p-1"><%=user.name%></h3>
                <h5 class="text-zinc-500 p-1"><%=user.email%></h5>
                <div class="flex gap-3 mt-10 p-1">
                <a  href="" class="text-zinc-400">Edit User</a>
                <a href="/delete/<%=user._id%>" class="text-red-500">Delete User</a>
                 </div> 
                </div>
                <%})%>
       <% }else{%>
       
        <h1 class=" text-3xl  mt-2 bold">No Users</h1>
        <%}%>
        
    </div>
  </div>
</body>
</html>
